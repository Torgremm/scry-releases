# scry releases

This repository contains compiled release artifacts for the private `Torgremm/scry` source repository.

The source code remains private. The `scry` command uses these public assets for `scry update`.
